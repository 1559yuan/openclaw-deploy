{
  "name": "openclaw-deploy",
  "version": "1.0.0",
  "scripts": {
    "start": "OPENCLAW_GATEWAY_PORT=$PORT openclaw gateway --allow-unconfigured"
  },
  "dependencies": {
    "openclaw": "latest"
  }
}
